services:
  app:
    build: ./app
    env_file:
      - ./app/.env
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - openclaw-data:/data

volumes:
  openclaw-data:
